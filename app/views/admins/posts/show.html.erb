<div class="container form-style-background">

  <%= link_to 'Повернутись до статей', admins_posts_path, class: "btn btn-light btn-lg" %> 
  <hr>
  <p class="lead"><%= @post_admin.body %></p>
  <hr>
  <% if user_signed_in? || admin_signed_in? %>
    <%= render 'disqus' %>
  <% end %>
</div>